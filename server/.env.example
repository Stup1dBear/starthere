# Server Configuration
SERVER_PORT=8080
GIN_MODE=release

# Database Configuration
DB_HOST=mysql
DB_PORT=3306
DB_USER=starthere
DB_PASSWORD=starthere_password
DB_NAME=starthere

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRY_HOURS=24

# Email Configuration
# Driver options: "log" (print to console), "smtp" (send real emails)
EMAIL_DRIVER=smtp
EMAIL_FROM_ADDRESS=no-reply@star-there.com
EMAIL_FROM_NAME=StartHere 星辰

# 阿里云邮件推送 SMTP 配置
SMTP_HOST=smtpdm.aliyun.com
SMTP_PORT=80
SMTP_USER=no-reply@star-there.com
SMTP_PASS=your-smtp-password-here

# Frontend URL (for email verification links)
FRONTEND_URL=https://star-there.com
